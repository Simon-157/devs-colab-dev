import { Key } from "react";

export interface ProblemPropType {
    description: string;
    id: number;
    title: string;
    difficulty_level: string;
    created_at: Date;
    updated_at:Date;
  }
  
  
